# pavbot_gps

Still need to buffer the parsing more so that partial packets aren't discarded if they still contain important information
Also need to test with the GPS to make sure it works (just need a battery pack to bring it outside)
